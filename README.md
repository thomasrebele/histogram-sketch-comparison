Histogram comparison
====================

## Displaying the diagrams

Start an HTTP server in the directory results/, e.g.

```
busybox httpd -v -f -p 127.0.0.1:8000
```

Then open a browser on that server (e.g., <a href="http://localhost:8000/">http://localhost:8000/</a>).
